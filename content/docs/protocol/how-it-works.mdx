---
title: How It Works
description: AARON splits biometric authentication into four phases — capture, proof, payment, and access.
icon: Cog
---

AARON splits biometric authentication into four phases — **capture**, **proof**, **payment**, and **access**.

## 1. Private Gaze Audit

AARON captures biometric tensors on-device using **MediaPipe face landmarks**. Raw iris data never leaves the validator node. The Agentive Gaze Tensor (AGT) system classifies eye movement into three tensors: **Cognitive (COG)**, **Emotional (EMO)**, and **Environmental (ENV)**. AGT performs the Web4 actions that power JETT Auth.

## 2. Opaque Proof to Solana

A **32-byte verification hash** is computed from the gaze sequence, hold durations, and session nonce. This hash is submitted on-chain via the **Proof-of-Attention DePIN** program in [jettoptx-poa-depin](https://github.com/jettoptx/jettoptx-poa-depin). Validators confirm the proof without ever seeing raw biometric data.

## 3a. x402 Unlocks Action

Payment attestation triggers domain registration, NFT mint, or agent delegation via micropayment. The **x402 protocol** settles payments atomically alongside the biometric proof on the Solana rail (`aaron.jettoptics.ai`).

## 3b. Machine-to-Machine Payments (MPP)

The MPP protocol enables autonomous agents to pay for AARON attestation services using **USDC** on Solana. Agent wallets submit x402 micropayments that are atomically settled alongside proof verification. Each agent payment can create an **AgentAcquisition PDA** on-chain, linking the payment to the authenticated user.

Vault program: `JTX5uXTiZ1M3hJkjv5Cp5F8dr3Jc7nhJbQjCFmgEYA7`

## 3c. Vault donations & receipts (Solana)

Production settlement is **Solana-native**. Donations (SOL or USDC) generate on-chain **DonorReceipt** PDAs via `jett_vault`. Cross-chain XRP / Xahau / Wormhole paths from earlier docs are **retired** — see [On-Chain Bridge](/docs/on-chain-bridge).

Receipt fields typically include donation amount, payment method, claim status, and any JTX / OPTX entitlements configured for the campaign.

## 4. Access tiers

**$JTX v2** on Solana mainnet gates product access (DOJO / JettChat tiers). **$OPTX** (devnet today) is the PoA reward / utility token. Higher stake or subscription tier → lower x402 fees + priority compute where enabled.

| Code tier | JTX v2 held | Notes |
|-----------|-------------|-------|
| `basic` | ≥ 1 | Entry gate |
| `mojo` | ≥ 12 | MOJO tier |
| `dojo` | ≥ 444 | DOJO tier |
| `spaceCowboy` | ≥ 1,111 **or** Space Cowboy NFT | Top tier + governance |

Live pricing and checkout: [astroknots.space/stake](https://astroknots.space/stake). Canonical mint: [On-Chain Addresses](/docs/getting-started/on-chain-addresses).
